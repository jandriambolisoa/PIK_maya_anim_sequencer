# PIK_maya_anim_sequencer

An alternative for the Maya's Camera Sequencer to create layout sequences.

## Requirements
- Maya 2026
- PIK_maya_quickBlast

## Launch the tool in Maya
Run this command :
`from PIK_maya_anim_sequencer.scripts.main import run; run()`